#lang scheme

(require "exercise-2.46.scm")

(require "frame-l.scm")
; (require "frame-c.scm")

;;; test
; (define f (make-frame (make-vect 1 2)
;                       (make-vect 3 4)
;                       (make-vect 5 6)))
; (origin f)
; (edge1 f)
; (edge2 f)

